import classes from "./Loading.module.css";

function Loading() {
  return (
    <div className={classes.LoadingCon}>
      <div className={classes.Loading}></div>
    </div>
  );
}

export default Loading;
